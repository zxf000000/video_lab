# ExecPlan: 测试视频生成效果 MVP

## 1. 目标

构建一个最小可运行项目，验证以下四步链路：

1. 文本提示生成剧情段落
2. 剧情拆分镜头
3. 镜头提示词生成首尾帧
4. 首尾帧加提示词生成视频

项目目标是快速验证效果，不追求完整商业化产品。

## 2. 范围

### In Scope

- 单用户使用
- 单项目工作台
- 中间产物可视化
- 单镜头重试
- 基础任务状态管理

### Out of Scope

- 多人协作
- 完整支付与订阅
- 长视频自动拼接成片
- 高级素材管理

## 3. 关键假设

- 文本生成、图像生成、视频生成能力由外部模型服务提供
- MVP 阶段允许人工调 prompt
- 不要求镜头间强一致性系统，只需跑通链路

## 4. 架构草案

### 前端

- 一个新建项目页
- 一个项目详情页
- 一个镜头详情抽屉或子页

### 后端

- REST API
- 异步任务执行器
- 任务状态持久化

### 数据层

- SQLite 或 Postgres
- 本地文件或对象存储保存结果文件地址

## 5. 里程碑

### M1：剧情生成闭环

- 完成项目创建页
- 接通剧情生成接口
- 支持查看与编辑剧情段落

验收标准：

- 用户能创建项目并拿到一段剧情文本

### M2：镜头拆分闭环

- 接通剧情拆镜头逻辑
- 展示镜头列表
- 支持修改单镜头文案

验收标准：

- 用户能基于剧情得到 3 到 6 个结构化镜头

### M3：首尾帧闭环

- 接通单镜头首尾帧生成
- 展示图片结果
- 支持单帧重试

验收标准：

- 用户能为至少一个镜头成功生成 start frame 和 end frame

### M4：视频生成闭环

- 接通基于首尾帧的视频生成
- 展示视频结果
- 支持失败重试

验收标准：

- 用户能为至少一个镜头成功生成视频片段

### M5：批量任务和可观测性

- 批量执行多个镜头任务
- 展示任务状态和错误信息
- 保留每一步输入输出

验收标准：

- 用户能看见完整链路状态，并能对失败镜头单独重试

## 6. 实施步骤

1. 定义核心数据模型：Project、StorySegment、Shot、FrameTask、VideoTask
2. 建立项目创建与详情 API
3. 实现剧情生成接口
4. 实现镜头拆分接口
5. 实现首尾帧任务接口与状态回写
6. 实现视频任务接口与状态回写
7. 完成前端工作台与结果展示
8. 补充日志、失败处理和重试

## 7. 风险

- 模型响应慢，影响体验
- 首尾帧风格不一致，导致视频质量不稳定
- 某些视频模型不稳定，失败率较高
- 单镜头 prompt 质量不足，导致测试结果失真

## 8. 风险缓解

- 所有步骤异步化
- 所有中间结果落库
- 保留 prompt 编辑入口
- 支持单步重试而非整项目重跑

## 9. 最小交付

最小交付标准不是“能做长视频”，而是：

- 可以创建一个项目
- 可以生成剧情
- 可以拆出镜头
- 可以为单镜头生成首尾帧
- 可以为单镜头生成视频
- 可以在页面上看到每一步结果

## 10. 下一步实现建议

优先做后端接口和任务状态模型，再做前端页面。因为这个项目的核心价值是验证链路，不是 UI 复杂度；如果状态模型先不稳定，前端做得再完整也没有意义。
