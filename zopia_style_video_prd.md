# 类 Zopia 视频生成平台产品文档

## 1. 文档信息

- 文档版本：v1.0
- 文档类型：PRD / 产品方案
- 适用阶段：0 到 1 产品设计
- 产品代号：Project Aurora
- 对标对象：Zopia 风格的 AI 视频生成平台

## 2. 产品概述

### 2.1 产品定位

Project Aurora 是一个面向个人创作者、小团队和 AI Agent 的视频生成平台。产品目标不是只生成单条视频片段，而是让用户以最低交互成本完成从创意输入、脚本拆解、角色设定、分镜设计、素材生成、镜头拼接到成片导出的完整流程。

一句话定义：

“把一句话创意自动转成可编辑、可迭代、可导出的完整视频项目。”

### 2.2 核心价值

- 降低视频创作门槛：用户不需要逐镜头写复杂提示词。
- 提升长视频可用性：解决角色一致性、镜头连贯性和结构完整性。
- 提高生产效率：把创意阶段、素材阶段和剪辑阶段串成同一个工作流。
- 支持 Agent 化生产：既支持人在页面操作，也支持智能体通过 API 自动发起任务。

### 2.3 产品形态

- Web 端创作平台
- API / Agent Gateway
- 可选桌面端包装

## 3. 产品目标

### 3.1 业务目标

- 在 3 分钟内让新用户完成第一次视频项目创建。
- 提高首日项目完成率。
- 提高“创意输入后生成成片”的闭环完成率。
- 形成可传播的公开视频结果，带动自然增长。
- 为后续会员、按量付费、API 调用打下计费基础。

### 3.2 用户目标

- 输入一句话，即可快速得到一个可看的视频草案。
- 允许中途修改脚本、角色、画风、旁白和节奏。
- 对生成结果不满意时，可以低成本局部重做，而不是整片推翻。

### 3.3 非目标

- 首期不做专业 NLE 级时间线编辑器。
- 首期不提供复杂影视团队协同权限系统。
- 首期不自研底层视频模型，优先聚合第三方模型能力。

## 4. 用户与场景

### 4.1 核心用户

#### 用户 A：短视频创作者

- 目标：快速产出剧情短片、情绪短片、营销片段
- 痛点：不会写复杂提示词，不会做镜头衔接，剪辑耗时

#### 用户 B：营销与品牌团队

- 目标：批量生成产品故事片、广告概念片、活动预热视频
- 痛点：素材制作成本高，改稿频繁，跨团队沟通成本高

#### 用户 C：AI 原生用户 / Agent 用户

- 目标：将视频生成功能嵌入自动化工作流
- 痛点：现有视频工具多为 UI 手工操作，不适合程序化调用

### 4.2 典型使用场景

- 输入一句产品文案，自动生成 30 秒广告片
- 输入一个故事梗概，生成 60 秒剧情预告片
- 输入角色设定和风格参考，生成连续镜头短剧
- 由 AI Agent 批量创建项目并自动生成视频素材

## 5. 产品原则

- 低门槛输入，高质量结构化输出
- 项目优先，而不是单素材优先
- 允许用户在任意阶段回退与局部重做
- 对生成链路保持可解释性，用户知道系统当前在做什么
- 默认自动化，关键节点保留人工干预

## 6. 核心产品逻辑

### 6.1 核心逻辑概述

平台围绕“项目”组织一切数据和任务。每个项目都遵循统一生产链路：

1. 用户输入创意
2. 系统生成项目设定
3. 系统编写脚本
4. 系统拆解为角色、场景、分镜和镜头任务
5. 系统调用图像/视频/语音模型生成素材
6. 系统完成初步剪辑与音频合成
7. 用户预览结果并进行局部修改
8. 系统重新生成局部内容或导出成片

### 6.2 与普通文生视频工具的差异

- 普通工具以“单次 prompt -> 单段视频”为中心
- 本产品以“项目 -> 多阶段 -> 多镜头 -> 成片”为中心
- 普通工具主要优化模型入口
- 本产品主要优化任务编排、一致性控制和结果交付

## 7. 用户旅程

### 7.1 新用户主旅程

1. 进入首页
2. 在输入框中输入一句话创意
3. 选择时长、风格、语言、比例
4. 点击“开始创作”
5. 系统创建项目并展示生成进度
6. 用户看到脚本草案和角色设定
7. 用户选择“直接生成”或“先编辑再生成”
8. 系统完成素材生成与自动剪辑
9. 用户在结果页预览成片
10. 用户可执行导出、分享、局部重做、复制项目

### 7.2 老用户复访旅程

1. 打开控制台
2. 查看项目列表和生成状态
3. 进入已有项目
4. 修改脚本或某个镜头
5. 对局部发起再生成
6. 导出新版本

## 8. 信息架构

### 8.1 页面结构

- 首页
- 创作输入页
- 项目生成中页面
- 项目工作台
- 导出页
- 项目列表
- 定价页
- API / Agent 文档页
- 账户与额度中心

### 8.2 项目工作台结构

- 顶部：项目名、状态、剩余额度、导出按钮
- 左侧：项目大纲导航
- 中部：脚本 / 分镜 / 预览切换区域
- 右侧：参数、提示词、风格、模型与再生成控制区

## 9. 功能模块设计

### 9.1 创意输入模块

#### 功能目标

让用户用最简单的输入启动项目。

#### 输入项

- 自由文本创意
- 视频时长
- 画面比例
- 风格模板
- 语言
- 旁白开关
- 背景音乐风格

#### 输出项

- 新项目记录
- 初始项目设置
- 一条标准化的创作任务

#### 设计原则

- 首页输入框必须极简
- 高级参数默认折叠
- 提供示例 prompt，降低首用门槛

### 9.2 项目初始化模块

#### 功能目标

把自然语言创意转成结构化项目数据。

#### 系统输出

- 项目标题
- 项目摘要
- 视频目标时长
- 故事类型
- 风格定义
- 目标镜头数
- 初始角色列表
- 初始场景列表

#### 说明

该阶段不直接生成视频，先建立项目骨架，为后续脚本和镜头阶段提供上下文。

### 9.3 脚本生成模块

#### 功能目标

自动形成一版完整脚本，作为后续镜头生成的中枢。

#### 输出结构

- 故事梗概
- 三段式结构或章节结构
- 场景列表
- 每场景的核心动作
- 台词与旁白
- 情绪与节奏说明

#### 用户操作

- 编辑脚本
- 重写脚本
- 指定更短 / 更长 / 更广告化 / 更剧情化

### 9.4 角色与世界观模块

#### 功能目标

保证多镜头中的角色和世界设定一致。

#### 输出结构

- 角色名
- 外观描述
- 穿着设定
- 性格关键词
- 声线
- 角色参考图
- 场景参考图

#### 关键逻辑

- 角色设定一旦确认，默认作为后续镜头生成的全局约束
- 用户可以锁定角色设定，避免再生成时漂移

### 9.5 分镜与镜头规划模块

#### 功能目标

将脚本拆解成可执行的视频镜头任务。

#### 每个镜头字段

- Shot ID
- 所属场景
- 镜头时长
- 景别
- 运镜方式
- 主体动作
- 背景环境
- 画面风格
- 配音文案
- 音效建议
- 依赖角色 / 场景资产

#### 设计要求

- 系统应自动给出镜头数量建议
- 用户可删除、合并、扩写单个镜头
- 用户可将单镜头标记为重点镜头，提高生成质量

### 9.6 素材生成模块

#### 功能目标

基于分镜生成图片、视频、音频等素材。

#### 子模块

- 角色图生成
- 场景图生成
- 单镜头视频生成
- 旁白生成
- 背景音乐生成或匹配

#### 关键策略

- 先生成关键帧，再生成视频，可降低失败率
- 对高成本步骤采用队列调度
- 对失败镜头自动重试，并保留失败原因

### 9.7 一致性控制模块

#### 功能目标

降低长链路生成中的人物漂移、画风漂移和剧情跳跃。

#### 控制方式

- 全局角色设定约束
- 场景参考图复用
- 镜头前后文注入
- 同一场景使用同一批参考资产
- 对已确认镜头进行“锁定”

#### 成功标准

- 同一角色在连续镜头中辨识度稳定
- 相邻镜头光线、服装、环境不出现明显断裂

### 9.8 自动剪辑模块

#### 功能目标

把离散镜头自动组装成可交付的视频草案。

#### 输出内容

- 镜头拼接结果
- 转场建议
- 字幕
- 背景音乐
- 旁白轨
- 简单音效

#### 用户可调项

- 节奏快慢
- 配音音色
- 背景音乐强度
- 是否自动加字幕

### 9.9 项目预览与局部重做模块

#### 功能目标

让用户不必推翻整个项目，只修改不满意部分。

#### 支持操作

- 重写某场景脚本
- 替换某角色形象
- 重做单个镜头
- 重做某段旁白
- 调整整体风格

#### 交互要求

- 修改后明确提示影响范围
- 对局部修改生成新版本，不覆盖历史版本

### 9.10 导出与分享模块

#### 输出能力

- 导出 MP4
- 导出带字幕版本
- 导出无水印版本
- 生成分享链接
- 复制项目

#### 导出限制

- 免费用户限制导出分辨率、次数或带水印
- 会员用户开放更高分辨率和更长时长

### 9.11 API / Agent Gateway

#### 功能目标

允许第三方程序和 AI Agent 在无 UI 的情况下调用平台能力。

#### 核心接口类别

- 创建项目
- 更新项目设置
- 发起脚本生成
- 发起镜头生成
- 查询任务状态
- 拉取结果地址

#### 典型使用方式

- Agent 根据用户需求自动创建视频项目
- 企业系统根据活动日历自动生成视频素材

## 10. 核心流程设计

### 10.1 主流程

1. 用户输入创意
2. 创建项目
3. 生成项目骨架
4. 生成脚本
5. 生成角色与场景资产
6. 生成分镜与镜头计划
7. 批量调度素材生成
8. 自动剪辑与音频合成
9. 项目预览
10. 局部修改或导出

### 10.2 状态机

项目状态建议如下：

- Draft：草稿，尚未启动生成
- Initializing：项目初始化中
- ScriptReady：脚本已生成
- AssetPreparing：角色/场景资产准备中
- ShotGenerating：镜头生成中
- Editing：自动剪辑中
- ReviewReady：可预览
- Exporting：导出中
- Completed：已完成
- Failed：失败
- PartialFailed：部分镜头失败，可局部重试

### 10.3 失败处理流程

- 如果脚本生成失败：允许重新生成脚本，不影响项目创建
- 如果个别镜头失败：标记失败镜头并可单独重试
- 如果自动剪辑失败：保留已生成镜头，允许重新执行剪辑
- 如果导出失败：保留预览版本，允许重新导出

## 11. 后台编排逻辑

### 11.1 多 Agent 分工建议

- Director Agent：理解用户目标，规划创作路线
- Script Agent：生成和改写脚本
- Character Agent：生成角色与场景设定
- Storyboard Agent：拆解分镜
- Generation Agent：调度图像、视频、音频模型
- Editor Agent：拼接镜头与音轨
- QA Agent：检查一致性、缺帧、字幕错误和结果完整性

### 11.2 编排策略

- 串行：项目初始化 -> 脚本 -> 分镜
- 并行：角色图、场景图、多个镜头生成任务
- 回写：每个阶段都把结构化结果回写到项目上下文
- 重试：对高成本步骤设置有限重试机制
- 降级：当高质量模型拥塞时，可自动切换普通模型

### 11.3 调度优先级

- 高价值用户任务优先
- 导出中的项目优先
- 局部重做优先于首次大批量生成
- 长视频任务拆批次执行，避免队列阻塞

## 12. 数据模型建议

### 12.1 Project

- project_id
- user_id
- title
- brief
- duration_target
- aspect_ratio
- language
- style_preset
- status
- created_at
- updated_at

### 12.2 Script

- script_id
- project_id
- summary
- structure_json
- narration_text
- version

### 12.3 Character

- character_id
- project_id
- name
- appearance_prompt
- personality_tags
- voice_profile
- locked

### 12.4 Shot

- shot_id
- project_id
- scene_id
- order_index
- duration
- shot_prompt
- camera_prompt
- asset_refs
- status
- version

### 12.5 RenderTask

- task_id
- project_id
- shot_id
- task_type
- provider
- cost
- status
- retry_count
- output_url

## 13. 页面交互要求

### 13.1 首页

- 一个主输入框
- 一个“开始创作”按钮
- 示例输入卡片
- 风格模板快捷选项
- 展示公开视频案例

### 13.2 生成中页面

- 显示当前阶段
- 显示已完成步骤
- 显示剩余预计时间
- 允许用户先看脚本和角色设定

### 13.3 工作台

- 脚本、角色、分镜、成片四个核心标签页
- 每个镜头单独可编辑和重做
- 明确显示额度消耗
- 支持版本切换

## 14. 计费与商业化建议

### 14.1 免费版

- 限制总生成额度
- 限制并发任务数
- 导出带水印
- 限制最长时长

### 14.2 专业版

- 更高额度
- 更高分辨率
- 更快队列
- 去水印
- 可使用高级模型

### 14.3 API / 企业版

- 独立计费
- 按调用量或按生成分钟数计费
- SLA 和优先队列
- 团队项目与权限能力

## 15. 风控与内容安全

### 15.1 风控重点

- 涉黄、涉政、暴力、侵权人物
- 深度伪造与名人肖像风险
- 品牌商标与版权音乐风险

### 15.2 风控方案

- 输入前审核
- 输出后审核
- 命中风险后进入人工复核或拒绝生成
- 企业版保留更严格审计日志

## 16. 核心指标

### 16.1 北极星指标

- 项目成片完成率

### 16.2 关键过程指标

- 首页到项目创建转化率
- 项目创建到首个预览产出率
- 镜头失败率
- 局部重做率
- 导出率
- 分享率
- 次日留存与 7 日留存

### 16.3 商业指标

- 免费到付费转化率
- 每用户平均额度消耗
- API 调用收入占比

## 17. MVP 范围建议

### 17.1 必做

- 一句话创建项目
- 脚本自动生成
- 分镜自动拆解
- 单项目多镜头生成
- 自动拼接预览
- 单镜头局部重做
- MP4 导出

### 17.2 可延后

- 多成员协作
- 时间线级精细编辑
- 素材市场
- 模板商店
- 手机端完整编辑能力

## 18. 版本规划建议

### V1

- 跑通从创意到成片的最小闭环
- 支持 15 到 60 秒视频
- 提供基础风格模板

### V1.5

- 加入角色锁定和风格锁定
- 加入局部重做与版本管理
- 开放基础 API

### V2

- 多 Agent 深化
- 企业工作台
- 团队协作
- 更强的一致性系统
- 支持连续短剧和系列视频

## 19. 竞品差异化建议

- 不只卖模型入口，要卖项目闭环
- 不只输出素材，要输出成片
- 不只支持网页操作，要支持 Agent 调用
- 不只支持一次生成，要支持项目级版本迭代

## 20. 一句话总结

这类产品的核心不是“让用户生成一个视频”，而是“让系统替用户完成一轮导演、制片、分镜、生成和初剪工作”。

如果要模仿 Zopia 的流程，最关键的不是页面长什么样，而是把视频生产从单步工具重构成项目驱动、多阶段编排、可局部重做、最终能导出成片的工作流系统。
