# ExecPlan: 参考 Dribbble 后台风格改造 Video Lab 前端

## 1. 目标

将当前 `Video Lab` 前端，从偏工程化的深色实验工具界面，改造成更接近参考图的 `浅色 SaaS Admin / Survey Builder` 风格，同时保证现有项目链路不变：

- 首页：项目列表与入口页
- 项目详情：剧本 / 角色&场景 / 分镜板 / 时间轴
- 单镜头详情页

目标不是简单“换皮”，而是把当前视频实验台包装成一套更可信、更易演示、更适合扩展的产品后台界面。

## 2. 风格判断

参考图的核心不是复杂动效，而是清晰的后台产品语言：

- 浅色主背景 + 白色卡片
- 非常轻的描边和阴影
- 紫色作为品牌强调色
- 左侧窄导航 + 顶部工具栏 + 主工作区
- KPI 卡片、表格、表单、状态标签并存
- 高信息密度，但通过卡片分组和留白维持秩序

对当前项目来说，应该借的是这些“结构语言”，不是逐像素抄图。

## 3. 当前现状

### 已有基础

- 前端已拆为 `Next.js + TailwindCSS`
- 页面结构完整，有首页、项目详情页、单镜头详情页
- 业务链路已经打通，按钮、任务、镜头、角色、场景、素材均可操作
- 组件已拆分出 `ProjectHeader`、`StepIndicator`、`ShotCard`、`TaskPanel`

### 当前问题

- 全局仍是深色实验风格，与参考图的浅色后台方向相反
- 页面以垂直堆叠为主，缺少“后台骨架感”
- 头部、导航、内容区层次不够稳定，像工具页，不像产品后台
- 卡片风格过于统一，缺少 KPI 卡、数据摘要卡、表单卡、任务卡的角色区分
- 首页和项目详情都缺少“概览层”，信息进入速度慢
- 时间轴和任务区有业务信息，但视觉承载方式还偏工程态

## 4. 设计策略

### 核心策略

保留现有业务组件和路由，只重做：

- 页面骨架
- 颜色与排版系统
- 卡片分层
- 顶部导航/左侧导航/工具条
- KPI 摘要区
- 表格与表单表现

### 不做的事

- 不重写后端
- 不引入新状态管理
- 不在本轮做复杂拖拽分镜编辑器
- 不做重动画或营销页化设计

## 5. 目标信息架构

### 首页

参考图中的 `Survey List / Dashboard` 视角，首页改为“项目总览后台”：

- 左侧窄导航
- 顶部搜索和环境状态栏
- 首屏 KPI 卡
- 下方项目列表表格/卡片
- 右上保留“新建项目”

建议首页结构：

1. 顶部栏
   - 产品标识
   - 搜索框
   - 配置状态
   - 个人入口 / 配置入口
2. KPI 摘要
   - 项目总数
   - 可用镜头数
   - 已生成帧数
   - 已生成视频数
3. 项目列表
   - 标题
   - Prompt 摘要
   - 状态
   - 更新时间
   - 进入项目

### 项目详情页

参考图中的 `Survey Details / Survey Creation / Dashboard` 混合布局，项目详情改为“主工作台”：

1. 顶部工具栏
   - 返回首页
   - 项目标题
   - 项目状态
   - 当前模型/环境信息
2. 顶部 KPI 行
   - 镜头总数
   - 总时长
   - 已生成帧镜头数
   - 已生成视频镜头数
3. 中部主工作区
   - 左：当前 Tab 内容
   - 右：项目摘要 / 最近任务 / 生成状态
4. Tab 导航
   - 剧本
   - 角色&场景
   - 分镜板
   - 时间轴

### 单镜头详情页

参考图中的详情表单页，单镜头页改为“镜头审阅面板”：

- 左侧：镜头元信息、Prompt、操作按钮
- 右侧：首帧、尾帧、视频结果
- 底部：生成记录 / 失败日志（后续可扩）

## 6. 视觉系统改造

### 颜色

当前：

- 深蓝绿背景
- 青绿色强调色

目标：

- 页面背景：暖灰白或冷灰白
- 卡片背景：纯白 / 极浅灰
- 主品牌色：柔和紫色
- 辅助色：浅黄、浅蓝、浅红用于状态和图表

建议 token 方向：

- `--bg-page: #f4f5f9`
- `--bg-surface: #ffffff`
- `--bg-muted: #f8f8fc`
- `--line: #e8e9f2`
- `--text-main: #1f2430`
- `--text-subtle: #7d8496`
- `--brand: #6f67d8`
- `--brand-soft: #efedff`
- `--success-soft: #edf8ee`
- `--warning-soft: #fff5df`
- `--danger-soft: #fff0f1`

### 排版

参考图是典型后台字重系统：

- 页面标题：中等偏粗
- 分区标题：中等
- 表单标签：细、小
- 表格正文：常规
- 数据数字：更强调

建议：

- 保留当前无额外依赖的系统字体方案
- 将标题字重统一收敛到 `600`
- 正文更多用 `text-sm`
- 次级说明固定为 `text-xs/text-sm + muted`

### 圆角和阴影

建议统一：

- 页面主卡：`24px`
- 子卡片：`18px`
- 按钮/输入框：`12px`
- 阴影很轻，只做浮层感，不做强立体

## 7. 组件级落地计划

### 7.1 全局 Layout

新增统一后台骨架组件，例如：

- `AdminShell`
- `SidebarNav`
- `TopToolbar`
- `StatCard`

作用：

- 首页、项目详情页、单镜头详情页统一使用一套后台容器
- 减少每页重复拼布局

### 7.2 `globals.css`

当前这是最先要改的文件：

- 取消深色背景和大面积 radial gradient
- 建立浅色全局 token
- 补充通用 `panel`、`surface`、`muted`、`brand` 类

涉及文件：

- `frontend/app/globals.css`

### 7.3 `ProjectHeader`

当前头部太像开发工具栏，改造方向：

- 左侧返回 + 项目名称
- 中间可放面包屑或副标题
- 右侧放环境状态和主操作

涉及文件：

- `frontend/src/components/ProjectHeader.jsx`

### 7.4 `StepIndicator`

当前更像流程条，参考图里更接近后台二级导航。

改造方向：

- 从“步骤条”转成“Tab Pills / Secondary Nav”
- 选中态用浅紫底
- 非选中态更克制
- 不再强调流程完成感，更多强调信息切换

涉及文件：

- `frontend/src/components/StepIndicator.jsx`

### 7.5 首页卡片和列表

首页建议从“项目列表”升级为：

- 顶部 4 个摘要卡
- 下方项目列表表格或大卡片列表

涉及文件：

- `frontend/app/page.jsx`
- 新增 `frontend/src/components/StatCard.jsx`
- 可新增 `frontend/src/components/ProjectListTable.jsx`

### 7.6 剧本 Tab

当前剧本 Tab 信息完整，但表现偏纯内容块。

改造方向：

- 左：剧情正文卡
- 右：项目状态卡 + 版本历史卡
- 头部按钮右对齐，主按钮更明确

涉及文件：

- `frontend/src/components/tabs/ScriptTab.jsx`
- `frontend/src/components/TaskPanel.jsx`

### 7.7 角色&场景 Tab

这是最适合贴近参考图表单/列表风格的区域。

改造方向：

- 角色和场景改成两块白卡
- 每条角色/场景用更清晰的媒体对象布局
- 右侧操作按钮统一为小号按钮组
- 提取和生成动作提升为卡片级工具条

涉及文件：

- `frontend/src/components/tabs/CharactersTab.jsx`

### 7.8 分镜板 Tab

当前每个 `ShotCard` 信息很多，但视觉上偏“开发测试面板”。

改造方向：

- 每张镜头卡分成：
  - 标题区
  - Prompt 区
  - 操作区
  - 产物预览区
- 首尾帧与视频结果做成规整素材卡
- 右上角状态标识改成轻量标签

涉及文件：

- `frontend/src/components/tabs/StoryboardTab.jsx`
- `frontend/src/components/ShotCard.jsx`
- `frontend/src/components/AssetCard.jsx`

### 7.9 时间轴 Tab

当前逻辑可用，但视觉形式过于功能化。

改造方向：

- 顶部大时间条保留
- 下方镜头状态列表改成更接近表格摘要
- 自动拼接区域做成 `Coming Soon` 辅助卡，而不是虚线占位块

涉及文件：

- `frontend/src/components/tabs/TimelineTab.jsx`

### 7.10 单镜头详情页

当前是纵向堆叠，建议改成两栏：

- 左栏：Prompt、操作、镜头信息
- 右栏：首帧、尾帧、视频

涉及文件：

- `frontend/app/projects/[id]/shots/[shotId]/page.jsx`

## 8. 分阶段实施

### Phase 1：视觉基础层

- 改 `globals.css`
- 建立浅色 token
- 统一按钮、输入框、卡片样式

验收标准：

- 整站从深色切到浅色后台风格
- 不改业务逻辑，只改观感

### Phase 2：骨架层

- 首页和项目详情页引入统一后台骨架
- 完成顶部栏、侧栏、KPI 卡布局

验收标准：

- 页面第一屏看起来像后台产品，不再像实验台

### Phase 3：业务内容层

- 剧本/角色/分镜/时间轴逐 Tab 改造
- 完善摘要卡、状态标签、任务卡

验收标准：

- 每个 Tab 形成清晰的信息分组
- 表单、表格、卡片角色分明

### Phase 4：镜头详情与细节层

- 单镜头页改为两栏工作台
- 统一按钮加载态、状态标签、素材卡
- 清理边界样式问题

验收标准：

- 镜头页与项目页视觉语言一致
- 演示链路完整且稳定

## 9. 推荐实施顺序

为了降低回归风险，建议按以下顺序执行：

1. `globals.css`
2. `ProjectHeader`
3. `StepIndicator`
4. 首页 `app/page.jsx`
5. 项目详情页主骨架 `app/projects/[id]/page.jsx`
6. `ScriptTab`
7. `CharactersTab`
8. `StoryboardTab` + `ShotCard` + `AssetCard`
9. `TimelineTab`
10. 单镜头详情页

## 10. 风险

### 风险 1：只换颜色，不改结构

结果会变成“浅色版旧页面”，但仍然不像参考图。

应对：

- 优先改骨架和卡片分层，不要只改 token

### 风险 2：过度追求展示稿效果

会造成页面好看但不实用，损失当前业务可操作性。

应对：

- 所有交互按钮、任务区、错误态必须保留

### 风险 3：一次性大改，容易引入回归

应对：

- 按 Tab 分阶段落地
- 每阶段都跑 `next build`
- 关键页面用 Playwright 回归

## 11. 验收标准

改造完成后，应满足：

- 首页、项目页、镜头页统一为浅色 SaaS Admin 风格
- 具备清晰后台骨架：顶部栏 + 主内容区 + 摘要卡
- 现有视频实验链路不受影响
- 所有关键任务按钮保留加载态
- 页面在桌面端和移动端都能正常阅读

## 12. 最小可交付版本

如果只做一轮最小落地，建议先交付以下内容：

- 浅色全局视觉系统
- 首页后台化
- 项目详情页后台化
- 分镜板卡片改造
- 单镜头详情页两栏化

这样就足够把当前项目从“内部实验页面”提升到“可演示产品原型”。

## 13. 当前结论

这套风格完全适合当前项目，但正确做法不是复刻截图，而是：

- 借它的后台结构
- 借它的浅色卡片系统
- 借它的紫色品牌表达
- 用你现有的视频生成链路去填充真实业务内容

如果继续执行，下一步应该直接进入 `Phase 1`，优先改 `frontend/app/globals.css`、`frontend/src/components/ProjectHeader.jsx`、`frontend/src/components/StepIndicator.jsx` 和 `frontend/app/page.jsx`。
