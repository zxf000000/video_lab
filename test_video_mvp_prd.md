# 测试视频生成效果小项目 PRD

## 1. 项目目标

这个项目的目标不是做完整的视频创作平台，而是做一个最小可验证系统，用来测试“剧情 -> 镜头 -> 首尾帧 -> 视频”的生成效果和链路稳定性。

核心原则：

- 先跑通完整链路，再优化画质和体验
- 先支持单项目单人使用，再考虑协作
- 先验证生成效果，再做复杂编辑能力

## 2. MVP 范围

### 2.1 核心功能

1. 通过文本提示生成指定剧情段落
2. 通过剧情拆分镜头
3. 通过镜头提示词生成视频首尾帧
4. 通过视频首尾帧以及提示词生成视频

### 2.2 非目标

- 不做完整时间线编辑器
- 不做复杂账户体系
- 不做高级多人协作
- 不做完整商业化计费
- 不追求一次性支持长视频

## 3. 核心用户

### 用户画像

- 想测试不同模型视频效果的个人开发者
- 想快速验证 AI 视频链路的产品经理
- 需要对比不同 prompt 和镜头结构效果的内容创作者

### 用户核心诉求

- 能快速输入一个故事创意并生成结构化结果
- 能看到每一步的中间产物，而不是只看最终视频
- 能方便地替换 prompt 后重新测试

## 4. 产品形态

建议采用一个轻量 Web 应用，包含以下页面：

- 首页 / 新建项目页
- 项目详情页
- 镜头详情页
- 任务状态页

MVP 不需要复杂导航，单项目工作台即可。

## 5. 核心流程

### 5.1 主流程

1. 用户输入剧情需求
2. 系统生成剧情段落
3. 系统将剧情段落拆为镜头列表
4. 系统为每个镜头生成结构化镜头提示词
5. 系统为每个镜头生成首帧和尾帧
6. 系统基于首帧、尾帧和镜头提示词生成视频
7. 用户查看每个镜头的结果并决定是否重试

### 5.2 关键设计

- 每一步都必须保留中间结果
- 每个镜头都能单独重跑
- 每一步都要显示输入 prompt 和输出结果
- 所有任务都要有状态，避免黑盒执行

## 6. 功能设计

### 6.1 功能一：剧情段落生成

#### 目标

让用户输入一句话或一段需求，系统输出一个结构完整、可用于分镜的剧情段落。

#### 输入

- 故事主题
- 风格
- 时长目标
- 主角设定
- 场景限制

#### 输出

- 项目标题
- 剧情简介
- 一个或多个剧情段落
- 推荐总时长

#### 示例输出结构

- 标题
- 故事背景
- 剧情段落正文
- 风格说明

#### 交互要求

- 用户可以手动编辑剧情段落
- 用户可以点击“重新生成”
- 用户可以选择“固定当前剧情，进入分镜”

### 6.2 功能二：剧情拆分镜头

#### 目标

把剧情段落拆成镜头级结构，便于后续首尾帧和视频生成。

#### 输入

- 剧情段落
- 目标镜头数量或自动拆分
- 单镜头建议时长

#### 输出

每个镜头至少包括：

- shot_id
- 镜头顺序
- 镜头描述
- 人物动作
- 场景描述
- 运镜描述
- 情绪关键词
- 镜头时长

#### 交互要求

- 用户可增删镜头
- 用户可改单个镜头文案
- 用户可对单个镜头重新拆解

### 6.3 功能三：根据镜头提示词生成首尾帧

#### 目标

为每个镜头生成起始画面和结束画面，作为视频生成的视觉约束。

#### 输入

- 镜头提示词
- 风格约束
- 角色描述
- 场景描述
- 可选参考图

#### 输出

- start_frame_url
- end_frame_url
- 对应生成参数

#### 设计要求

- 起始帧和结束帧要分别展示
- 支持单独重生成 start frame
- 支持单独重生成 end frame
- 支持锁定一侧帧，只重做另一侧

### 6.4 功能四：根据首尾帧和提示词生成视频

#### 目标

使用起始帧、结束帧和镜头 prompt 生成单镜头视频片段。

#### 输入

- start_frame
- end_frame
- shot_prompt
- 时长
- 比例
- 运动强度

#### 输出

- video_url
- 生成状态
- 任务耗时
- 使用模型和参数

#### 交互要求

- 可查看生成日志
- 可单镜头重试
- 可更换 prompt 重新生成

## 7. 页面设计

### 7.1 首页 / 新建项目页

字段建议：

- 项目名
- 故事需求输入框
- 风格选择
- 视频比例
- 目标总时长
- “生成剧情”按钮

### 7.2 项目详情页

模块建议：

- 剧情段落卡片
- 镜头列表
- 每个镜头的状态
- 当前总任务进度
- “一键为全部镜头生成首尾帧”
- “一键为全部镜头生成视频”

### 7.3 镜头详情页

模块建议：

- 镜头文本描述
- prompt 编辑框
- 首帧预览
- 尾帧预览
- 视频预览
- 重试按钮

### 7.4 任务状态展示

每个步骤建议统一状态：

- pending
- running
- succeeded
- failed

## 8. 数据结构建议

### 8.1 Project

- id
- title
- story_prompt
- style
- aspect_ratio
- target_duration
- status
- created_at

### 8.2 StorySegment

- id
- project_id
- content
- version
- status

### 8.3 Shot

- id
- project_id
- order_index
- shot_description
- shot_prompt
- duration_seconds
- status

### 8.4 FrameTask

- id
- shot_id
- type: start | end
- prompt
- image_url
- status
- model_name

### 8.5 VideoTask

- id
- shot_id
- start_frame_url
- end_frame_url
- prompt
- video_url
- status
- model_name

## 9. 后台任务流程

### 9.1 推荐任务拆分

- story_generate_task
- shot_split_task
- frame_generate_task
- video_generate_task

### 9.2 编排方式

- 剧情生成：串行
- 镜头拆分：串行
- 各镜头首尾帧生成：并行
- 各镜头视频生成：并行

### 9.3 失败处理

- 某个镜头失败不影响其他镜头
- 某个首帧失败时，只重试该帧
- 某个视频任务失败时，保留首尾帧结果

## 10. 推荐技术方案

### 10.1 前端

- Next.js 或 React 单页应用

### 10.2 后端

- Node.js / Python 任一都可以
- 提供 REST API 即可

### 10.3 存储

- SQLite 用于 MVP
- 本地文件或对象存储保存图片、视频结果

### 10.4 队列

- MVP 可先用数据库轮询或轻量队列
- 后续再升级为 Redis 队列

## 11. API 草案

### 11.1 创建项目并生成剧情

- `POST /api/projects`

请求：

- title
- story_prompt
- style
- aspect_ratio
- target_duration

### 11.2 拆分镜头

- `POST /api/projects/:id/split-shots`

### 11.3 生成镜头首尾帧

- `POST /api/shots/:id/generate-frames`

### 11.4 生成镜头视频

- `POST /api/shots/:id/generate-video`

### 11.5 查询项目详情

- `GET /api/projects/:id`

### 11.6 查询镜头详情

- `GET /api/shots/:id`

## 12. MVP 成功标准

- 用户可在 10 分钟内跑通至少一个完整项目
- 至少能成功生成一个剧情段落
- 至少能成功拆分出 3 到 6 个镜头
- 至少有一个镜头能成功产出首尾帧
- 至少有一个镜头能成功基于首尾帧生成视频

## 13. 建议实现顺序

### Phase 1

- 项目创建
- 剧情生成
- 剧情手动编辑

### Phase 2

- 镜头拆分
- 镜头列表展示
- 单镜头 prompt 编辑

### Phase 3

- 首尾帧生成
- 图片结果展示

### Phase 4

- 视频生成
- 视频播放与重试

### Phase 5

- 批量执行
- 日志与任务状态完善

## 14. 一句话总结

这个小项目的本质不是做“完整 AI 视频平台”，而是做一个可观察、可重试、可替换 prompt 的视频生成实验台，用最短路径验证“剧情 -> 镜头 -> 帧 -> 视频”的链路质量。
