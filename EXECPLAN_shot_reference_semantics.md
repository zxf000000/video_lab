# ExecPlan: 分镜图号语义校验与自动纠偏

## 1. 目标

修复分镜拆解后的 `start_frame_prompt` / `end_frame_prompt` 中图号语义漂移问题，确保 `图一/图二/...` 只能稳定映射到角色和场景，而不会被模型错误地重绑定到车身、路面、霓虹等局部物体。

## 2. 范围

### In Scope

- 在 `shots` 提示词中进一步强调图号只能代表角色和场景，不能代表局部物体
- 在 `ChatfireProvider` 的镜头归一化阶段增加图号语义校验
- 对不合法的 `start_frame_prompt` / `end_frame_prompt` 进行自动重写或回退
- 统一上下游图号格式，避免 `图一/图二` 与 `图1/图2` 混用
- 增加针对图号语义校验和自动纠偏的测试

### Out of Scope

- 改造数据库 schema
- 新增前端分镜编辑 UI
- 对历史项目中的已生成图片或视频做批量迁移

## 3. 核心问题

- 模型虽然收到“图号对应角色和场景”的提示，但仍可能把 `图一/图二` 当作任意视觉槽位
- 运行时当前只检查首尾帧 prompt 是否非空，不检查图号后的描述是否还在引用角色或场景
- 下游图片生成会优先使用模型返回的首尾帧 prompt，导致错误语义直接进入出图请求
- 分镜提示词使用 `图一/图二`，而图片请求参考图标签使用 `图1/图2`，会放大模型理解偏差

## 4. 实施方案

1. 在 `video_lab/prompts/shots/system.txt` 中增加硬约束：
   - `图一/图二/...` 只能引用角色和场景
   - 不得写成“图一车身倾斜”“图二路面反光”这类局部物体描述
   - 局部物体、姿态、光线变化必须写在图号绑定后的角色/场景描述后面

2. 在 `ChatfireProvider` 增加图号语义检查函数：
   - 根据 `character_ids` 和 `scene_name` 计算合法图号映射
   - 检查 `start_frame_prompt` / `end_frame_prompt` 是否使用了合法图号集合
   - 检查每个图号后的首个语义锚点是否仍指向角色名或场景，而不是物体片段

3. 增加自动纠偏策略：
   - 如果首尾帧 prompt 缺失，继续沿用现有 fallback
   - 如果首尾帧 prompt 非空但图号语义不合法，直接重写为规范版本
   - 重写时保留镜头动作和氛围信息，但重新固定角色/场景图号绑定

4. 统一图号表达：
   - 优先统一为中文序号 `图一/图二/...`
   - 图片生成层参考图说明也改为同一套编号，减少上下游不一致

5. 补充测试：
   - 模型返回合法图号 prompt 时，应原样保留
- 判定规则：当 `图一/图二/...` 后的首个指代对象不是该图号对应的角色名或场景，而是“车身、路面、霓虹、手指、门把手”等局部物体、身体局部或环境碎片时，判定为图号语义错绑
- 重写目标：自动改写为“图号先绑定角色或场景，再补充该角色/场景上的姿态、物体状态、构图关系或光线变化”的结构
- 重写示例：将“图一车身倾斜，图二路面霓虹反射”改写为“图一中的角色身体前倾、重心失衡，图二中的街道路面映出破碎霓虹反光”；如果该镜头无角色，则改写为“图一中的街道路面映出破碎霓虹反光”
   - 无角色场景下，场景应稳定使用 `图一`
   - 多角色场景下，角色与场景顺序应稳定不乱

## 5. 验收标准

- `start_frame_prompt` / `end_frame_prompt` 中的图号只再表示角色和场景
- 非法图号语义在运行时会被自动修正，不再直接进入图片生成请求
- 图片生成请求里的参考图标签与分镜 prompt 使用同一套图号体系
- 相关测试通过，覆盖合法保留与非法重写两类路径

## 6. 风险与缓解

- 风险：语义校验过严，误判部分本来可用的 prompt
  缓解：优先做“明显错误”检测，只拦截图号后直接跟局部物体或环境碎片的情况

- 风险：自动重写过强，损失模型原本写出的有效细节
  缓解：重写时复用原 `shot_prompt`、`scene_description`、`character_action` 中的动作和氛围信息，只重置图号绑定结构
