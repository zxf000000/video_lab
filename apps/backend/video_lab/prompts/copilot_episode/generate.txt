请基于下面的上下文，根据项目的 Brief 和角色设定生成分集大纲。

分集生成要求：
1. 从 brief 的世界观（world_rules）、主冲突（main_conflict）和反转规则（reversal_rules）推导出分集结构。
2. 每集必须有独立的冲突弧和爽点，集与集之间要有递进关系。
3. 角色出场要利用已有的 character 设定，保持人物一致性。
4. opening_hook 要具体有画面感，ending_hook 要让人想看下一集。
5. 生成的集数参考项目的 episode_count_planned，如果未指定则根据剧情复杂度生成 6-12 集。
6. 如果用户在「用户目标」中提供了补充描述（如"需要一个反转结局"），必须优先体现。
7. 最终必须严格按 system prompt 要求输出 `===PROPOSAL===` 包裹的纯 JSON，不能使用 Markdown 代码块。

项目 ID: {project_id}
实体 ID: {entity_id}
用户目标:
{user_goal}

上下文:
{context_json}