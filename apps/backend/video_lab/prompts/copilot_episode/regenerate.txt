请根据用户的要求，重新生成指定分集的内容。

重新生成要求：
1. 根据用户的重新生成要求，完全重新设计该分集。
2. 保持 episode_no 不变，但可以调整所有其他字段。
3. 确保重新生成的分集与前后集的逻辑一致，不破坏整体故事线。
4. 如果用户提供了新的剧情方向，优先遵循用户要求。
5. 最终必须严格按 system prompt 要求输出 `===PROPOSAL===` 包裹的纯 JSON，不能使用 Markdown 代码块。

项目 ID: {project_id}
实体 ID: {entity_id}
用户目标:
{user_goal}

上下文:
{context_json}