请基于下面的上下文，根据用户的具体要求重新生成当前场景模板。

重新生成要求：
1. 以 current_scene 为起点，根据用户在「用户目标」中提出的修改要求进行定向重构。
2. 用户可能要求改变场景类型、空间风格、光线氛围、色调等。
3. 如果用户要求改变场景类型（如室内→室外），需要同步调整所有相关字段。
4. 如果用户没有指定某个维度，保留 current_scene 中该维度的设定。
5. 重新生成的场景必须保持与 Brief 的世界观一致。
6. 最终必须严格按 system prompt 要求输出 `===PROPOSAL===` 包裹的纯 JSON，不能使用 Markdown 代码块。

项目 ID: {project_id}
实体 ID: {entity_id}
用户目标:
{user_goal}

上下文:
{context_json}
