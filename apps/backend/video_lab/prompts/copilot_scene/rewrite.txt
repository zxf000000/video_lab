请基于下面的上下文改写当前场景模板，让场景更鲜明、更适合短剧视觉生成。

改写要求：
1. 保留场景的核心空间定位，优化视觉描述。
2. 强化空间结构、光线、色调和关键道具的描述。
3. 确保 image_prompt 可直接用于生成场景参考图。
4. 保证场景与 Brief 的世界观一致。
5. 最终必须严格按 system prompt 要求输出 `===PROPOSAL===` 包裹的纯 JSON，不能使用 Markdown 代码块。

项目 ID: {project_id}
实体 ID: {entity_id}
用户目标:
{user_goal}

上下文:
{context_json}
