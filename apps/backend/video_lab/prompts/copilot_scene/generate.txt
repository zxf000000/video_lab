请基于下面的上下文，根据项目的 Brief 生成一组场景模板。

场景生成要求：
1. 从 brief 的世界观（world_rules）和主冲突（main_conflict）推导出短剧需要的核心场景。
2. 场景必须服务于剧情，覆盖短剧的主要空间类型（如：居住空间、工作空间、冲突场所、情感场所等）。
3. 每个场景需要包含完整的视觉设定：空间描述、光线、色调、关键道具。
4. 场景之间要有视觉差异，避免重复。
5. 生成的场景数量建议 3-8 个，取决于剧情复杂度。
6. 如果用户在「用户目标」中提供了补充描述（如"需要一个雨夜街头场景"），必须优先生成这些场景。
7. 最终必须严格按 system prompt 要求输出 `===PROPOSAL===` 包裹的纯 JSON，不能使用 Markdown 代码块。

项目 ID: {project_id}
实体 ID: {entity_id}
用户目标:
{user_goal}

上下文:
{context_json}
