请基于下面的上下文，根据剧本中的新场景位置生成场景预设。

场景生成要求：
1. 根据 unmatched_locations 列表中的位置名称为每个位置生成一个场景预设。
2. 每个场景需要包含完整的空间结构设定：空间布局、关键建筑元素（入口、门、窗、墙）、光线、色调。道具仅在剧情必需时列出。
3. 场景之间要有视觉差异，避免重复。
4. 生成的场景数量应等于 unmatched_locations 的数量。
5. 如果用户在「用户目标」中提供了补充描述，必须优先体现。
6. 最终必须严格按 system prompt 要求输出 `===PROPOSAL===` 包裹的纯 JSON，不能使用 Markdown 代码块。

项目 ID: {project_id}
实体 ID: {entity_id}
用户目标:
{user_goal}

上下文:
{context_json}
