请基于下面的上下文补全当前角色资产中缺失的信息。

补全要求：
1. 如果 generation_stage 是 `profile_collection`，补全整组角色卡的缺失信息，重点补 character_profile。
2. 如果 generation_stage 是 `visual_refine`，只输出 1 个角色，并重点补齐 image_spec 缺失字段。
3. 优先补齐缺失字段，不要无意义重写已有字段。
4. 让补全部分与 Brief、现有角色关系和主冲突一致。
5. 如果上下文不足，也要给出最稳妥、最符合题材的角色设定。
6. 最终必须严格按 system prompt 要求输出 `===PROPOSAL===` 包裹的纯 JSON，不能使用 Markdown 代码块。

项目 ID: {project_id}
实体 ID: {entity_id}
用户目标:
{user_goal}

上下文:
{context_json}
