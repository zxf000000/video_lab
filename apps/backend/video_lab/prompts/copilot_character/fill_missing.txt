请基于下面的上下文补全当前角色资产中缺失的信息。

补全要求：
1. 如果 generation_stage 是 `profile_collection`，补全整组角色卡的缺失信息，重点补 character_profile。
2. 如果 generation_stage 是 `visual_refine` 且 `current_variant` 为空，只输出 1 个角色，并重点补齐 image_spec 缺失字段。
3. 如果 generation_stage 是 `visual_refine` 且 `current_variant` 不为空，只输出 1 个 variant，并重点补齐 image_spec_override 缺失字段。
4. 优先补齐缺失字段，不要无意义重写已有字段。
5. 让补全部分与 Brief、现有角色关系和主冲突一致。
6. variant 模式下只补当前形态的变化字段，不要改动基础脸型与身份感。
7. 如果上下文不足，也要给出最稳妥、最符合题材的角色设定。
8. 最终必须严格按 system prompt 要求输出 `===PROPOSAL===` 包裹的纯 JSON，不能使用 Markdown 代码块。
9. **素描风格出图**：补齐 image_spec 时，image_prompt 必须以 "colored pencil, line art, flat colors, solid background." 开头，描述色铅笔线稿风格，平涂色块，纯色背景。negative_prompt 必须排除 realistic, photographic, 3D render, oil painting, digital painting。

项目 ID: {project_id}
实体 ID: {entity_id}
用户目标:
{user_goal}

上下文:
{context_json}
