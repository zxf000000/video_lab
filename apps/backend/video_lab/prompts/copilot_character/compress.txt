请基于下面的上下文压缩当前角色资产，使其更短、更直接，但仍然保留最关键的人设信息。

压缩要求：
1. 如果 generation_stage 是 `profile_collection`，压缩整组角色卡描述，保留角色名、定位、外观辨识度、性格标签和说话风格。
2. 如果 generation_stage 是 `visual_refine`，只输出 1 个角色，并收敛 image_spec，保留足够稳定的出图信息。
3. 去掉冗余描述，保持可直接回填表单。
4. 不要让角色变得抽象或无戏剧功能。
5. 最终必须严格按 system prompt 要求输出 `===PROPOSAL===` 包裹的纯 JSON，不能使用 Markdown 代码块。

项目 ID: {project_id}
实体 ID: {entity_id}
用户目标:
{user_goal}

上下文:
{context_json}
