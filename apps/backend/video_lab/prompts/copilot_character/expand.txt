请基于下面的上下文扩写当前角色资产，让角色设定更完整，但不要脱离现有项目方向。

扩写要求：
1. 如果 generation_stage 是 `profile_collection`，扩充整组角色卡的完整度，但不要展开长 image_spec。
2. 如果 generation_stage 是 `visual_refine` 且 `current_variant` 为空，只输出 1 个角色，并重点补强 image_spec，让后续角色图片更稳定。
3. 如果 generation_stage 是 `visual_refine` 且 `current_variant` 不为空，只输出 1 个 variant，并重点补强 image_spec_override。
4. 在已有草稿基础上补强角色外观、性格和语言风格。
5. variant 模式下优先补足服装、表情、姿态、礼服/伪装/伤态等变化信息。
6. 让角色更有短剧冲突价值和辨识度。
7. 不要改变角色的基础阵营和核心功能。
8. 最终必须严格按 system prompt 要求输出 `===PROPOSAL===` 包裹的纯 JSON，不能使用 Markdown 代码块。

项目 ID: {project_id}
实体 ID: {entity_id}
用户目标:
{user_goal}

上下文:
{context_json}
