# ExecPlan: 提示词体系优化与运行层对齐

## 1. 目标

优化 Video Lab 的提示词体系，使其在以下方面更稳定：

- 结构化输出更容易被解析
- 分镜时长和字段约束更明确
- 首尾帧与视频生成的连续性更强
- 提示词变量定义与运行时代码保持一致

## 2. 范围

### In Scope

- 重写 story / rewrite / shots / characters / scenes / frame / video 提示词
- 修正 `PROMPT_VARS` 与实际模板变量不一致问题
- 优化 `ChatfireProvider` 对镜头输出的归一化与 fallback
- 补充针对分镜字段完整性和提示词变量的测试

### Out of Scope

- 更换模型供应商或请求协议
- 调整数据库 schema
- 改造前端 Prompt 编辑页交互

## 3. 核心问题

- `shots` 提示词过长，JSON 输出约束与质量建议混杂
- 首尾帧的“图一/图二”引用规则在多角色场景下不明确
- frame / video 提示词偏向画质形容词堆叠，缺少连续性与排他性约束
- `PROMPT_VARS` 未覆盖部分真实模板变量
- provider 成功解析 JSON 后的补全策略过弱，容易留下不可用字段

## 4. 实施方案

1. 将分镜提示词改为“输出目标 / 硬约束 / 质量要求 / 最短自检”结构
2. 明确图序号规则：角色按 `character_ids` 顺序编号，场景固定为最后一个图号；无角色时场景为图一
3. 为 frame / video 增加身份一致性、动作连续性、禁止新增主体等约束
4. 在 provider 中统一补全缺失字段，保证 start/end frame prompt、角色、场景、运镜、时长等字段始终可用
5. 为配置层和测试补齐新变量及约束

## 5. 验收标准

- Prompt 编辑接口返回的变量定义与模板实际使用一致
- `split_story_into_shots` 在 API 返回不完整 JSON 时仍能补成可用镜头对象
- fallback 生成的镜头包含首尾帧、角色、场景和稳定的时长总和
- 相关测试通过

## 6. 风险与缓解

- 风险：提示词约束过多导致模型输出变短或僵硬
  缓解：只保留高优先级硬约束，把建议压缩为少量规则

- 风险：provider 归一化逻辑与模型输出语义冲突
  缓解：仅补缺失值，不覆盖模型已有有效内容
